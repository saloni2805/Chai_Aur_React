import { createContext } from "react";

// Global userContext
const UserContext = createContext();

export default UserContext

// context sobt ek provider sudha bnvyla lagto, which will provide the data